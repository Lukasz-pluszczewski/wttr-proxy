# Wttr proxy

### Install dependencies
`npm i`

### Run development server
`REDIS_HOST=localhost REDIS_PORT=6379 PORT=8080 npm run dev`

### Run production server
`REDIS_HOST=localhost REDIS_PORT=6379 PORT=8080 npm start`
